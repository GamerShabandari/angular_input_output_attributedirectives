import { SetdurationDirective } from './setduration.directive';

describe('SetdurationDirective', () => {
  it('should create an instance', () => {
    const directive = new SetdurationDirective();
    expect(directive).toBeTruthy();
  });
});
